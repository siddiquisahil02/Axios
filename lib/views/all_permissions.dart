import 'package:flutter/material.dart';

class AllPermissions extends StatelessWidget {
  const AllPermissions({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text('All Permissions'),
        ),
        body: Container(
          padding: EdgeInsets.all(20),
          child: Text("alalal"),
        )
    );
  }
}
